# ผลทดสอบ V9.5 — Owner Login

วันที่ 20 กันยายน 2026 รันจริงในเครื่องทดสอบ ก่อนนำไป deploy

| ชุดตรวจ | ผล |
|---|---|
| Node: สูตร บันทึก และบัญชี | 29/29 ผ่าน |
| Playwright Chromium | 72/72 ผ่าน (36 กรณี × 2 ขนาดจอ) |
| ชุดข้อมูลแผนผัง | ตรวจ invariants ครบ 1,008 ชุด และ golden case 7–5–1 |
| จอ | 1366×768, 1920×1080 |
| เบราว์เซอร์ | Chromium 153.0.8010.0, headless |
| ภาพหน้า Login | ภาพแนบจริง 2048×1365; กล่องขวาไม่ทับเศียรครู |
| สูตรและ JavaScript UI เดิมก่อนส่วน save | เปรียบเทียบกับ HTML ที่ผู้ใช้แนบ: เหมือนเดิมทุกตัวอักษร |
| CSS เดิม | เหมือนเดิมทุกตัวอักษร; CSS Login เพิ่มแยกตาม ID |
| ภาพแนบ | ไบต์เหมือนต้นฉบับ ไม่มีการแก้ไขภาพ |
| ค่า Username/Password ในไฟล์ส่งมอบ | ว่าง ไม่มีบัญชีเริ่มต้น |

## หลักฐานการยืนยันตัวตน

- แฮช PBKDF2-HMAC-SHA256 600,000 รอบใน GAS เทียบกับ Node crypto สำหรับรหัสภาษาไทยและ Unicode ยาวแล้วตรงกัน
- ไม่ตั้งบัญชี / ไม่มี token / token ปลอม → ไม่เขียน Sheet
- รหัสถูก → สร้าง session และยกเลิก session ก่อนหน้า; ไม่เก็บรหัสผ่านตรง ๆ หรือ raw token ใน Script Properties
- ชื่อผู้ใช้ผิดหรือรหัสผิด → ข้อความเดียวกัน ไม่เปิดกระดาน
- ขีดจำกัดลองผิด 5 ครั้ง/15 นาที ใช้ Properties และ ScriptLock; เปลี่ยน username ไม่ข้าม limit
- Logout / หมดอายุ / เจ้าของ revoke / ตั้งบัญชีใหม่ → token เดิมบันทึกต่อไม่ได้
- ปลอมค่า sessionStorage → frontend ตรวจ server ก่อนเปิดกระดาน
- Login → ประมวลผล → บันทึก → บันทึกซ้ำ → refresh → logout ทดสอบผ่านหน้าเว็บจริงโดย route ไปเรียก Code.gs จริงภายใน Node VM
- สิทธิ์ถูกยกเลิกขณะกรอก → กลับหน้า Login เก็บฟอร์มและ request_id → Login ใหม่แล้วบันทึกได้
- นาฬิกาฝั่งหน้าเว็บครบกำหนด → ซ่อนกระดาน ล้าง session คงข้อมูลกรอกที่ยังค้าง

## Regression ที่คงไว้

ชุด UI เดิม 54 กรณีในสองขนาดจอ: สร้างแผนผัง, คะแนน 7 กลุ่ม, ไฮไลต์เลข/ฐาน8–9, แนวทแยง, เปลี่ยนอายุ, ฐานกราฟ12ใบ, คีย์บอร์ด, ชื่อ accessibility, ลดการเคลื่อนไหว, ไม่มีป้ายบังไพ่, ยืนยันบันทึก/รับมือ timeout/คำตอบผิด/รหัสคำขอซ้ำ และส่งออก PNG

4 กรณีส่งออกใช้ html2canvas 1.4.1 จริง ตรวจลายเซ็น PNG ขนาดและการคืนสภาพ UI

## ขอบเขตที่ยังต้องทดสอบหลัง deploy

บริการ Google (SpreadsheetApp, PropertiesService, LockService) จำลองใน Node VM ไม่ใช่รันใน Google Apps Script จริง จึงยังไม่ได้ยืนยันความเร็ว Login/โควตา/สิทธิ์ OAuth/พฤติกรรม CORS ของ deployment ใหม่ในบัญชีเจ้าของ

ภาพไพ่และการเรียก CDN ในชุดทดสอบใช้ fixture/local copy; ภาพ Login ใช้ของจริง ไม่ได้ตรวจภาพไพ่จากเว็บจริงในรอบนี้ ไม่ได้ทดสอบ Firefox/Safari หรือรับรอง WCAG ทั้งหมด

ไม่มีการ POST ไปยัง GAS จริง ไม่มีการเขียนลง Sheet จริง และไม่ได้เปลี่ยน GitHub หรือ deployment ของเจ้าของโดยอัตโนมัติ

## ไฟล์หลักฐาน

- verification/node-test.txt
- verification/ui-test.txt
- verification/unchanged.json (ผลเปรียบเทียบต้นฉบับและ SHA-256)
- playwright-report/index.html
- verification/Login-1366.png และ Login-1920.png

รอบแรกพบข้อผิดพลาดในตัวทดสอบเอง: กำหนดความกว้างภาพ 1920 แทนไฟล์จริง 2048 และ fixture อ้างตัวแปรที่ไม่ได้ import; แก้ตัวทดสอบแล้วรันชุดสุดท้ายครบตามผลด้านบน

# ผลตรวจ V9.5 ฉบับปรับปรุง — 20 กันยายน 2026

## ผลที่รันจริง

| การตรวจ | ผล |
|---|---|
| Node tests: โค้ด สูตร และตรรกะยืนยันการบันทึก | ผ่าน 20/20 ชุด (รันใหม่หลังปรับให้ใช้ A–D เดิม) |
| Chromium: UI / การยืนยันบันทึกแบบจำลอง / PNG | ผ่าน 54/54 รายการ (49.4 วินาทีในรอบสุดท้าย) |
| ขนาดหน้าจอ | 1366×768 และ 1920×1080 |
| สภาพแวดล้อม | Chromium 153.0.8010.0 headless, Playwright 1.62.1, Linux |
| ภาพ PNG | ใช้ html2canvas 1.4.1 จริง ตรวจการดาวน์โหลด ลายเซ็นไฟล์ PNG และขนาดภาพ 4 กรณี |
| ภาษาบนหน้าจอ/PNG | ใช้ Sarabun/Kanit จากแพ็กเกจฟอนต์ในเครื่องระหว่างทดสอบ |
| ป้ายที่บังไพ่ | ไม่มี .card-a11y-status บนหน้าเว็บ คง aria-label และชื่อภพเดิม |
| Dropdown 22 เรื่อง | ไม่มี UI/state/API ที่ถูกถอดออก |
| คะแนนชุดเลข 1–7 | แสดงครบและอยู่ในช่วง 0–100 |
| สูตร 9 ฐาน | Golden Test 7–5–1 และ invariants ข้อมูลตั้งต้น 1,008 ชุดผ่าน |

ผล UI 54/54 ด้านบนเป็นผลรอบก่อน; รอบปรับ Code.gs นี้ไม่ได้เปลี่ยน HTML และไม่ได้รัน UI ซ้ำ
เพิ่มการทดสอบคงข้อมูล A–D เดิม, ตรวจ readiness ของแท็บ/หัวคอลัมน์ และหยุดเมื่อไม่พบแท็บที่ระบุ

รายงานที่สร้างโดย Playwright อยู่ใน `playwright-report` พร้อมไฟล์ PNG แนบ
เปิดด้วย `npm run test:report` หลังติดตั้ง dependencies ตาม README

## สิ่งที่ชุด UI ตรวจ

| หมวด | สถานการณ์ |
|---|---|
| ฟอร์ม | ข้อมูลไม่ครบ ปีเกิดนอกช่วง สร้างกระดาน และข้อความผิดพลาด |
| การใช้งาน | สลับโหมด ไพ่ด้วย Enter/Space ฐาน 8–9 กรองไพ่ แนวทแยง |
| อายุ/โฟกัส | เปลี่ยนอายุคงข้อมูล คืนโฟกัส เปิด/กลับฐานกราฟ12ใบ |
| Accessibility | ชื่อช่อง รูปภาพ skip link เส้นโฟกัส และ reduced motion |
| ป้ายไพ่ | ไม่มีป้ายระดับบนภาพ แต่คำอธิบาย Screen Reader ยังมี |
| Google Sheet จำลอง | คำยืนยันตรงรหัส, ปลายทางเก่า, ข้อมูลว่าง, รหัสผิด, verified=false, คำตอบขาดหาย, ส่งซ้ำหลังรีโหลด, HTML แทน JSON, timeout |
| การส่งออก | เส้นทางสำเร็จ/ล้มเหลวแบบจำลอง และการสร้าง PNG ด้วย html2canvas จริง |

## ฝั่ง Apps Script ที่ตรวจแบบ Unit Test

ตรวจการ flush/อ่านแถวกลับ, ส่งซ้ำไม่เพิ่มแถว, รหัสเดิมแต่ข้อมูลต่างกัน,
การอ่านกลับไม่ตรง, lock ไม่ว่าง, ไม่มีค่าตั้งต้น, บริการผิดพลาด,
ป้องกันข้อความกลายเป็นสูตร, คำขอไม่ถูกต้อง และไม่ทับหัวคอลัมน์เดิมที่ต่าง schema

ชุดทดสอบใช้บริการ SpreadsheetApp/LockService จำลองใน Node
ไม่ได้รันใน Apps Script ของ Google และไม่ได้เขียน Google Sheet จริง

## สิ่งที่แก้ระหว่างทดสอบ

- แก้ขั้นตอนทดสอบ Shift+Tab ให้ย้อนจากช่องที่โฟกัสอยู่จริง ไม่สั่ง locator ให้ย้ายโฟกัสกลับก่อน
- ระบุ reduced-motion emulation ในกรณีทดสอบโดยตรง เพื่อยืนยันว่ากำลังทดสอบโหมดลดการเคลื่อนไหวจริง
- เพิ่มฟอนต์ไทยในสภาพแวดล้อมทดสอบ เพื่อไม่ใช้ภาพหน้าจอที่ภาษาไทยแสดงเป็นช่องสี่เหลี่ยม
- แก้สภาพแวดล้อม Chromium จนรัน UI และดาวน์โหลด PNG จริงได้; ผลก่อนติดตั้งสำเร็จไม่ถูกนับเป็นผลทดสอบผ่าน

## ข้อจำกัดที่ยังเหลือ

| เรื่อง | สถานะ |
|---|---|
| Google Sheet จริง | ปรับ Code.gs ตามโค้ดเดิมแล้ว ยังต้องตั้งค่า/Deploy และกำหนด Sheet เป้าหมายก่อน; ยังไม่ได้ยืนยัน end-to-end จริง |
| สิทธิ์และ CORS ของ deployment จริง | ยังไม่ได้ตรวจ; หากอ่านคำตอบไม่ได้ หน้าเว็บจะไม่แจ้งสำเร็จ |
| ภาพไพ่ต้นฉบับ | ใช้ภาพไพ่จำลองในการทดสอบ เพราะไม่มีโฟลเดอร์ images เดิม |
| ความพร้อม CDN จริง | ใช้ dependency/font ในเครื่องระหว่างทดสอบ จึงไม่รับรองเครือข่าย CDN |
| Firefox/Safari/Edge จริง | ยังไม่ได้รัน; Chromium ไม่ใช่หลักฐานว่าทุกเบราว์เซอร์ผ่าน |
| WCAG ทั้งหมด / Screen Reader จริง / ซูมทุกระดับ | ยังไม่ได้รับรอง |
| มือถือ / 22 หัวข้อ | ไม่นับเป็นขอบเขตตามคำสั่งของเจ้าของระบบ |

การมีใบยืนยันจำลองที่ถูกต้องพิสูจน์การจัดการสถานะของหน้าเว็บ แต่ไม่ใช่หลักฐานว่าข้อมูลเข้าบัญชี Google จริง
อ่าน `google-apps-script/SETUP.md` ก่อนใช้งานปุ่มบันทึกกับปลายทางจริง
